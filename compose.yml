name: pounce

services:
  pounce:
    build: .
    container_name: pounce
    restart: unless-stopped
    user: "1000:1000"
    network_mode: host
    volumes:
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro
